export enum OpCode {
    PUSH_CONST,
    PUSH_INT,
    PUSH_FLOAT,
    PUSH_TRUE,
    PUSH_FALSE,
    PUSH_NULL,
    POP,
    DUP,
    SWAP,

    ADD,
    SUB,
    MUL,
    DIV,
    NEG,
    MOD,

    EQ ,
    NEQ,
    GT,
    LT,
    GTE,
    LTE,
    NOT,

    JMP ,
    JZ  ,
    JNZ ,
    CALL,
    RET ,
    FUN,
    AND,
    OR ,

    LOAD_GLOBAL,
    STORE_GLOBAL,
    LOAD_LOCAL,
    STORE_LOCAL,
    LOAD_ARGS,

    PRINT_LN,
    INPUT,

    ARRAY_CREATE,
    ARRAY_GET,
    ARRAY_SET,

    STRING_CONCAT,
    STRING_LENGTH,
    STRING_SUBSTR,
    STRING_CHAR_AT,
    STRING_INDEX_OF,
    STRING_TO_INT,
    INT_TO_STRING,
    STRING_COMPARE,

    CLASS_DEF,
    OBJECT_NEW,
    FIELD_GET,
    FIELD_SET,
    METHOD_CALL,
    INSTANCEOF,

    TRY_BEGIN,
    TRY_END,
    CATCH,
    THROW,
    FINALLY,

    EXIT,
}
